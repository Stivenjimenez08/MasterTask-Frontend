import {LayoutPages} from '../Layout/LayoutPages'
import { Typography } from '@mui/material'

export const Taskpage = () => {
  return (
    <LayoutPages>
        <Typography sx={{mt:10}}>TaskPage</Typography>
    </LayoutPages> 
 
  )
}

