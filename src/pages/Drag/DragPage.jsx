import {LayoutPages} from '../Layout/LayoutPages'
import { Typography } from '@mui/material'

export const Dragpage = () => {
  return (
    <LayoutPages>
        <Typography sx={{mt:10}}>DragPage</Typography>
    </LayoutPages> 
 
  )
}