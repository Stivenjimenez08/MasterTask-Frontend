import {LayoutPages} from '../Layout/LayoutPages'
import { Typography } from '@mui/material'

export const Calendarpage = () => {
  return (
    <LayoutPages>
        <Typography sx={{mt:10}}>CalendarPage</Typography>
    </LayoutPages> 
 
  )
}